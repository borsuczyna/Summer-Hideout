export interface ParsedOptions {
    branch: string;
    help: boolean;
}
export declare const enum BoilerplateFeatures {
    WEB_STORM = "webstorm",
    VSCODE = "vscode",
    GITHUB = "github",
    EXAMPLE_RESOURCE = "example"
}
export interface PromptResults {
    projectName: string;
    continue: boolean;
    putProjectNameInSquareBrackets: boolean;
    features: BoilerplateFeatures[];
}
export declare function getBoilerplateUrl(branch: string): string;
export declare function camelToKebabCase(str: string): string;
export declare function newProject(rootDirectory: string, targetDirectory: string, options: ParsedOptions, promptResults: PromptResults): Promise<void>;
export declare function newProjectEntrypoint(args: string[]): Promise<void>;
