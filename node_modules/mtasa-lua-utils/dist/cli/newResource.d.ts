export interface ParsedOptions {
    branch: string;
    help: boolean;
}
export interface PromptResults {
    resourceName: string;
}
export declare function newResource(rootDirectory: string, promptResults: PromptResults): Promise<void>;
export declare function newResourceEntrypoint(args: string[]): Promise<void>;
