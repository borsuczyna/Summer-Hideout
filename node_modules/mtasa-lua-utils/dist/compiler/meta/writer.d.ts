import { MTASAMeta } from './types';
import { Element } from 'xml-js';
import { ResourceData } from '../utils';
import { Diagnostic } from 'typescript';
export declare const LUA_LIB_FILENAME = "lualib_bundle.lua";
export declare function generateResourceMetaObject(meta: MTASAMeta, data: ResourceData): Element;
export declare function generateResourceMetaContent(meta: MTASAMeta, data: ResourceData): {
    content: string;
    diagnostics: Diagnostic[];
};
